# Review provenance ledger — which commits have been through a review pass

> **Why this file exists**: mirrors MeshForge's `.claude/audits/review_provenance.md`
> (born 2026-07-05). Scoping "what's unreviewed" cost a full exploration pass
> because review provenance lived only in commit bodies. One row per review
> pass, updated when a pass closes. A commit range NOT in this table has had
> **no** adversarial review — that absence is the signal this ledger makes
> legible. Convention: record the RANGE + scope paths + fix commits + where the
> residual/refuted notes live. Never delete rows; supersede them.
>
> **MeshAnchor-specific note**: MeshAnchor was extracted from MeshForge
> (2026-04-01) and shares much of its structure. When MeshForge (the lead repo)
> reviews a shared-shape subsystem, the relevant findings are PORTED here rather
> than re-reviewed from scratch — those rows say "PORT of MF <date>" in the
> Mechanism column and only cover the ported findings, NOT a full independent
> audit of the MeshAnchor file.

| Date | Scope (range + paths) | Mechanism | Fix commits | Residuals / refuted notes |
|------|----------------------|-----------|-------------|---------------------------|
| 2026-09-18 | **PORT of MeshForge `58a98470` — CLOSES the handler-menu residual queued in MA's row below, same day.** `src/launcher_tui/backend.py` (`_AUTO_CANCEL` + `BACK_TAG` + `_infer_cancel_label`), `tests/test_dialog_backend.py` (+8). | **Byte-equivalent mechanism, MA-measured population.** MA has **214** menu call sites, ~167 carrying a `back` row. Per-call-site labelling was rejected in the lead repo after measurement: the 'None means back' contract is spelled at least five different ways, so no regex classifies the sites reliably — but the presence of the `back` ROW is unambiguous. Label is now DERIVED from `choices`, so the row and the button come from ONE source and cannot disagree. Verified HERE, not inherited: inference drilled RED (2 tests fail with it disabled), tree restored byte-identical (md5 `e5861a03`); **live through real whiptail under a pty with NO explicit label -> `2 "Back"` (row + button), ZERO `"Cancel"`**; full suite 6855 passed / 17 skipped exit 0 (6847 before, +8 = the new tests, re-derived); lint 0; `tui_smoke` 24x80 exit 0; affected set across tests/ AND scripts/ 236 passed exit 0. | (this row) | **Coverage: 8 menus -> ~167, with ZERO call-site edits.** **One MA-SPECIFIC test the lead repo does not need**: `test_the_main_menu_still_says_cancel`. MA's top level has NO `back` row, so inference correctly does not fire — which MATTERS here because Escape at MA's top level still counts as a dialog FAILURE. Without the pin, a future reader adding a `back` row to the main menu would unknowingly grant it a button that does not keep its word. **STILL OPEN — `DialogError` (MF review F7)**: unchanged by this pass. MA cannot distinguish a dead dialog from a user cancel; both return `None`. Needs the type threaded through `_run_main_menu`, and until then MA's main-menu Cancel stays unlabelled by design. **RESIDUAL, deliberate**: the 8 explicit `cancel_label=BACK_LABEL` on section menus are now redundant (inference yields the identical value); left rather than churn same-day commits. **Inherited residual risk**: the lead repo read only a SAMPLE of the functions carrying a back row without a regex-recognisable `None`->back mapping (MA has 23 such call sites across 10 files, NOT individually read here either) — BELIEVED-safe, not verified. |
| 2026-09-18 | **PORT of MeshForge `54880d62` (TUI audit Phase 5 — the escape hatch is CHROME) + MF review F4.** `src/launcher_tui/backend.py` (menu cancel semantics + `_cancel_flag`), `src/launcher_tui/main.py` (`BACK_LABEL` + the 8 SECTION call sites), `tests/test_dialog_backend.py` (new), `tests/test_tui_menu_guards.py` (call-site guards), and the four widened test doubles (`tests/test_cross_section_rows.py` x2, `tests/test_tui_menu_guards.py`, **`scripts/tui_smoke.py`**). | **Twin-mapped port, NOT blind-copied — `backend.py` is not parity-tracked, and each MF finding was checked against MA's actual code before porting.** MEASURED first with real whiptail (`tui_smoke --sizes 24x80`): MA's `mesh_networks` paints **17 of 20** (that section is `_optional_gateways_menu` — the key was kept for handler compatibility after the MeshCore-primary rework), and `_build_section_menu` appends `("back", "Back")` LAST unconditionally, so `back` is one of the three rows that falls off. Every other section fits. Three mutation drills RED (section loses its label; the MAIN menu GAINS one; a hardcoded `"Back"` literal), tree restored byte-identical (md5 `5c0ff39b`). F4 drilled RED against the pre-fix backend via `git stash`, restored byte-identical (md5 `60aea661`). Full suite 6847 passed / 17 skipped exit 0; lint 0; `tui_smoke` 24x80 exit 0 with row counts UNCHANGED (nothing moved). | `19e4693d` (F4: Cancel/Escape are answers), `4b48ae81` (the label) | **F4 half**: MA carried the ORIGINAL blanket retry — `menu()` re-ran the dialog on ANY non-zero exit, and whiptail returns 1 for Cancel / 255 for Escape, so every Cancel and every Escape on every submenu needed TWO presses. Safe to stop retrying because `_run()` has had `termios.tcflush()` all along (backend.py:107) — the retry was compensating for a defect that no longer existed. **⚠️ QUEUED — MA has no `DialogError`, so a dead dialog impersonates a user cancel (MF review F7).** MF RAISES `DialogError` when the dialog is still dead after its one retry; MA returns `None`, which is also what Cancel/Escape return, so `main.py` cannot tell 'user left' from 'dialog died' — its own comment says exactly that. **Not portable as-is**: `DialogError` exists NOWHERE in MA (`grep` across `src/ tests/ scripts/` = zero) and no caller catches one. Porting MF's main-menu 'offer Exit on None' WITHOUT it is actively worse — on a genuinely dead dialog it would offer an exit dialog that also fails, `continue`, and loop, trading a bounded `_MAX_DIALOG_RETRIES` counter for an unbounded one. Needs its own pass: add the type, raise after the failed retry, thread a catch through `_run_main_menu`, keep the counter as backstop. **Live consequence**: MA's main menu still counts a user's Escape as a dialog FAILURE and logs a warning for it; the main-menu Cancel button is therefore deliberately left unlabelled, pinned by `test_the_main_menu_is_deliberately_unlabelled`. **⚠️ QUEUED — the handler-menu tier is UNMEASURED and unfixed (BOTH repos).** This port covers the 8 section menus in `main.py`. MA has **~119 handler-level menus across 60 handler files** that carry their own `("back", "Back")` row and treat `None` as back — identical wiring, same two-faced control. `tui_smoke` renders only the 12 registry SECTIONS, so whether those handler menus scroll their `back` row off at 24x80 is **UNKNOWN, not fine**. MeshForge carries the same residual at **~117 menus / 68 files** and its `54880d62` fixed only 10. Fixing the class is ~236 call sites across both repos and deserves a design pass (per-call keyword vs. a section-menu helper), not a bolt-on. ⚠️ Handler menus that are operational PICKERS must keep 'Cancel' — cancelling an operation is not navigating back — so this is not a blind sweep. |
| 2026-09-17 | **PORT of MeshForge's profile-gating review (MF `eca86534` + `4128cc7b`, FRONTIER PASS 2026-09-16) — cross-section rows get ONE declaration.** `src/launcher_tui/{handler_registry,main}.py`, `scripts/tui_smoke.py`, `tests/test_cross_section_rows.py` (new), README + 3 docs stat sentinels. | MEASURED before building (operator-confirmed on the box): under `meshcore` (rns/meshtastic off) the primary menu listed NomadNet / Channels as available (2-tuples, never marked) and the keypress refused. Port of the MF alias design; MA's own tests drive the REAL `_meshcore_primary_menu` with the real MESHCORE profile (no monkeypatch needed — the flags are genuinely off); two mutation drills RED (alias never refuses → 2 failed; a per-loop fallback returns → 2 failed; files restored byte-identical); `tui_smoke` 24x80 real whiptail ungated vs `--profile meshcore` 117/120 identical; full suite 6,828 passed / 17 skipped (the one red was the README sentinel for the new test file, updated); lint 0. An adversarial review pass (code-reviewer agent, Opus) on `452c6c8f` ran BEFORE deploy — 6 findings, 5 acted on in `e157a7a4`, 1 recorded. | `452c6c8f`, `e157a7a4` | **Five rows, not three**: dashboard/network→system/network (owner unflagged, stays unmarked), meshcore/nomadnet→mesh_networks/nomadnet (rns), meshcore/meshchatx→rns/meshchatx, meshcore/channels→configuration/channels (meshtastic), configuration/rns-config→rns/edit (rns). ⚠️ **Visible change on meshanchor-server**: those rows carry their OWNER's label now (e.g. 'Launch MeshChatX'), and under meshcore NomadNet / Channels / RNS Config render `[off] …` with a refusal titled by the same label. NOT done here (MA's W7-class debt, out of scope): the shadowed same-section legacy lists (configuration 7 rows, system 7, about 5, dashboard 2, optional_gateways 3) still exist and are filtered on every render; the meshcore loop's `optional_gateways` opener is a legitimate legacy row and stays. The Pri-2 overloaded-profile row below touches the same seam and is untouched by this. Marker drawn on-terminal: MA's `tui_smoke` asserts it (unlike MF's). **REVIEW OF THE PORT (CONFIRMED, all 6)**: (1) HIGH — a SIXTH row: `rns_menu.py` carried its own nomadnet label + `dispatch("mesh_networks","nomadnet")` fallback, already drifted ('Launch NomadNet' vs the owner's 'NomadNet Client'); both guard tests read only main.py so could not see it → declared `rns/nomadnet`, copy + fallback deleted, guards scan every handler source and both quote styles, a test drives the REAL `_rns_submenu`. (2) `owner_row` None (owner registered, live rows dropped the tag) failed OPEN under a profile — unmarked, uncounted, RUN → now marked, counted, refused with a drift-naming in-app dialog; no profile = runs as before. ⚠️ **MF carries the identical fail-open branch** (`handler_registry.py` dispatch/get_menu_items, latent: no MF handler has a conditional `menu_items()`) — owed back to MF, not rolled tonight. (3) `alias()` silently overwrote a second declaration → ValueError. (4) startup log said action(s) while counting rows. (6) `test_all_handlers_protocol`'s registry never declared the rows → now does. (5) RECORDED, not changed: label legibility — the owner's 'Edit Reticulum Config' is unpadded on a two-column screen, and Dashboard's row now reads 'Network Tools' not 'Network Status'; one name per action is the design, owner labels are the owners' to pad. |
| 2026-08-13 | `7b43b46b^..f1d82ed4` (563e5d21 BrokenPipe guards + counters timeout marker; 0f8419bb SELF_HTTP_PORT self-recursion guard + registration; f1d82ed4 end-to-end self-dial pin; 7b43b46b count-only-what-judged) — reviewed as part of MF's 2026-08-13 frontier pass (see MF's row of the same date for the full inventory) | Frontier (Fable 5) inline adversarial read from the MF session: steer attacks on SELF_HTTP_PORT (multi-bind / test-order / import-shape / fail-open default) | none needed — range held | **Fail-OPEN RATIFIED**: registration is wired at both bind sites before serve_forever + pinned by `TestSelfPortRegistrationIsWired`; the unregistered case is exactly the cross-process caller (TUI `collect_fleet_rollup`) where the fetch is legitimate on a genuine co-install, and on the MA-owns-:5000 box that cross-process fetch now merges NOTHING (the server's own guard strips the MF blocks from the body it serves) — bounded, self-healing, one wasted localhost fetch per rollup. REFUTED: dual-module-instance via `src.` imports (docstrings only). RESIDUALS: header-write path (`send_response`/`end_headers`) still unguarded against hang-ups (twin-consistent with MF; the payload write dominates the storm); a port-0 ephemeral bind would register 0 and disarm the guard (test-only shape). **QUEUED — the cadence port gap (the biggest MA finding of the pass)**: MF fb7920d5 sized each user timer's window from its own cadence (`utils/timer_cadence.py` + outcome gate); MA's `check_user_timer_unit_failing` still judges against the FLAT 3h window, so a daily/weekly timer that fails every firing stays invisible (min_failures=2 unreachable), AND it counts a unit judged from a SINGLE witnessed outcome (one in-window failure reads healthy via `len(fails) < min_failures`). Shape-tier port needing its own drill cycle in MA's `HealthResult` architecture — NOT a byte-port. Exposure: MA-only standalone boxes; meshanchor-server itself is covered by MF's watchdog (fleet member). Also note MF's same-pass `timer_cadence` fixes travel WITH the port (UTC-timezone parse, transient-failure-not-memoised, RandomizedDelaySec last-wins). |
| 2026-07-24 | `src/utils/fleet_truth.py` (byte-locked twin) + `src/mini_dudeai/rollup.py` — the claw-cell + posture-pane half of MeshForge's dude-claw / fleet-monitor pass | PORT of MF 2026-07-24 (`/code-review` adversarial, 8 findings, all confirmed by hand); MA-side verification = `tests/test_fleet_truth.py` 44 passed exit 0, lint 0, parity in sync | `3790ebd7` (MF twin `41f34b4b`) | Ported findings: `_claw_cell.claw_count` counted ABSENT claws ("2 claws healthy" for one claw + a missing primary tick); all-absent now benign `absent=True`; new `claw_present` so a NOC panel cannot invent a claw from an unreachable box; rollup read only the primary tick so a dead second claw was invisible, and a no-mini box's claw was never collected though the renderer always drew one. NOT ported (MF-only surfaces): `web/fleet.html` claw panel, `web/node_map.html` claw cards, `map_federation`, `promote_seed_rules`, `battery_soak`, and the `utils/claw_battery` follow-on — MA has no claw panel or claw probes. Full inventory in MF's row of the same date. |
| 2026-07-06 | maps HTTP hardening + honest_status watchdog leg — `914de08b..b31a7daf` (src/utils/map_http_handler.py, src/utils/_map_fleet.py, scripts/honest_status.sh) | **PORT of MeshForge 2026-07-05 maps-domain QA audit** (adapted to MA's structure, NOT blind-copied) + a diagnosis pass on MA's honest_status. NOT an independent adversarial review of MA's whole map. | `a8a70627` (CORS tail-anchor + LAN-trust gate on /fleet/logs + /fleet/run-test), `b31a7daf` (honest_status watchdog leg → /fleet/blackouts) | PORTED: the shared `origin.startswith` CORS subdomain-bypass (→ tail-anchored `_origin_allowed`) + the ungated /fleet/logs (journal leak) + /fleet/run-test (open-LAN) → LAN-trust `_reject_if_untrusted`. **Deliberately NOT loosened**: `/api/radio/message` + PUT toradio stay `_is_localhost` (loopback-only) — MA is STRICTER than MF here (MF gates radio-TX to LAN-trust; the two repos intentionally differ), test-pinned. **DoS/clamp findings PORTED 2026-07-06 (this commit)**: limit clamp (`?limit=abc` uncaught-500 / `?limit=-1` unbounded), snapshot-window clamp (≤3600), open-redirect Host-validation (`/mesh`), weather-cache lock + single-flight, path-traversal `relative_to` (was `startswith`), destination 32-bit clamp — all 6 defects existed in MA identical to MF pre-fix; +11 tests (`test_map_dos_clamp_port`), MA map/http suite 894 passed. **honest_status fix**: the watchdog leg read a phantom `/var/lib/meshanchor/watchdog.json` (reader with no writer, honest_failure #4 — MF port artifact); rewired to the live `/fleet/blackouts` with a dead-watchdog UNKNOWN guard. +8 tests (`test_map_cors_trust_port`). MA suite 5461 passed; honest_status 6/6 green. Deployed + live-verified on meshanchor-server (CORS bypass blocked / legit reflected / LAN /fleet/logs 200). |
| 2026-07-06 | MA collector + fleet-observability + prometheus layer — FIRST INDEPENDENT adversarial pass (not a port). `dd716d36~1..2243a65e` (src/utils/{map_data_collector,_map_collector_meshtastic,_map_collector_rns,_map_collector_meshcore_public,node_history,prometheus_exporter,map_metrics,_map_fleet}, src/monitoring/{fleet_collector,fleet_aggregator,fleet_history,fleet_watchdog}) | 2 xhigh code-reviewer agents (collectors+node_history; fleet+prometheus) — MF-findings PORT-CHECK + fresh MA-specific review → 24 findings; **self-re-review of the fix commits** (feedback_review_your_own_fixes) → 1 fix-introduced issue caught+fixed (⚠️ mid-review I misjudged agent liveness by output-file mtime and wrongly killed one agent — re-launched; lesson: mtime ≠ liveness) | `dd716d36` (collectors), `b00a1bc9` (fleet/prometheus), `2243a65e` (re-review: B-9 None-return needed a 2nd consumer guarded in _serve_fleet_tests_list) | COLLECTOR: 8/9 shared MF findings PRESENT+fixed (CLI numeric-id → _canonical_meshtastic_id; future-clamp + _coerce_epoch at _is_node_online SSOT; record_observations coord float-coerce; source-dedup `(f.get('properties') or {})` guard; RNS last_heard-stamp + coord-validation + one-axis-phantom; meshtasticd lock-contention→skip-CLI; atomic _save_cache) + MA-specific: meshcore_public UNBOUNDED resp.read() OOM (48MB cap) + settings _safe_int. FLEET/PROMETHEUS: heartbeat-write-lost-on-one-bad-peer-field (false fleet-silent alarm); watchdog CLOSING valid daemon_dead blackout on empty table (→ _daemon_dead_reason evaluated both paths); unbounded peer resp.read() (128MB cap); _list_timers_scope []-masks-failed-probe → None; MF008 raw systemctl → check_service; prometheus POST-body cap; write_to_file fixed-tmp → mkstemp; negative-age not counted active; heartbeat ts guard+clamp. **REFUTED** by the reviewers: prometheus label-VALUE escaping IS handled (metrics_common), WAL+busy_timeout IS set (connect_tuned) — so raw-injection + basic write-contention are not live. +25 tests; full MA suite 5484 passed; lint 0. **DEFERRED (low/perf, in ledger not yet a task)**: prometheus per-node env/health label cardinality cap (B-F6), per-scrape collector-object reuse (B-F7), SERVICE_UP dead gauge (B-F11), PromQL substring-filter silent-no-data (B-F13); collector SQL-vs-Python origin-priority drift (F10), get_nodes_without_position unlocked read (F11-collector), meshcore id-space consistency (F12, conditional on node.id format). |
| 2026-07-19 | fleet-truth honesty invariants, MA side — `491fff34` port surface (src/utils/fleet_truth.py byte-identical builder, src/utils/fleet_truth_collector.py MA shim, _map_fleet._serve_fleet_truth) | Frontier (Fable 5) adversarial pass run in the LEAD repo: self-pass on the invariant core + 6 refutation-framed verifier agents + fresh-eyes renderer/collector attacker; MA receives the reviewed builder as a byte-identical mirror + the collector fixes adapted to MA idiom. Full findings table + residuals live in MeshForge's review_provenance row of the same date (lead-repo convention). | `c16a240e` (⚠️ SHA corrected 2026-08-06: originally cited `0afc8add`, the pre-amend image of this same commit — amending the provenance row INTO the fix commit changed the SHA the row had just cited; not reachable from `main`) | MA-specific CONFIRMED+fixed: bounded response read (8 MB / 10s wall — drip-peer can't hang the fan-out lock), http.client.HTTPException caught (garbage HTTP → dark box, not worker-error), monotonic TTL (NTP backstep can't freeze the doc as fresh), IP-shaped fleet.json peer-NAME masking (MF014/15), fanout.membership fleet-of-one transparency. Builder fixes inherited via byte-mirror (stale-signals-not-active, stale-mini-escalations, _ci_cell observed-states-only, services core-taint, box_state roll-up). **RESIDUAL → RESOLVED same-day**: `fleet_config.non_self_peers` bare-hostname drop + IP-self double-count fixed by positive-identity self-detection (resolve → local-bind ownership test, memoized; identity-unknown → KEEP, fail-visible — never a silently dropped row); +5 tests incl. the real bind-probe primitive. The 0-total-coverage residual also closed same-day by the MA status enrichment (`1df8178f`: blackout kinds = MA's enum). |
| 2026-07-28 | **MF adversarial-review port — the 4 of 10 findings with MA twins** (`scripts/honest_status.sh` suite/SHA/watchdog legs + fleet_hosts HOME guard, `scripts/pytest_tmp_prune.sh` exit contract + pinned lock window). The review itself ran on the MF side (MF ledger row 2026-07-28, fixes MF `f7811c02`): 8 finder angles over `3afda33a~1..HEAD`, ~40 candidates verified to 10; meta-pattern was the session's fixes re-committing broken-mapped-to-absent one door over. | **Fable 5 frontier pass on MF; twin-mapped port here** — each finding checked for an MA twin and for which way the fix travels before porting: 4 ported, 6 refuted-as-no-twin (no fleet_pull deployer → no resolver lib, awk-$1 already handles comments; no dups probes → no operator_cron_wired / fallback-reason twins; `_find_operator_user` use in `check_user_timer_unit_failing` is a different, documented healthy-with-reason self-guard). | `b7c23e9f` (⚠️ SHA corrected 2026-08-06: originally cited `60437c38`, the pre-amend image of this same commit — same class as the other correction of this date) (verified: MA suite 6009 passed / 14 skipped exit 0, lint exit 0, 4 shell harnesses ALL PASS incl. the new `tests/test_honest_status_boxes.sh` pinning the SHA/watchdog classifications) | **Residuals**: MA's watchdog leg keeps `not-found` units as unverifiable/UNKNOWN (MA has no legitimately-absent-organ tier — every configured box should run the MA watchdog; revisit if that changes). MA's fleet_hosts chain stays 2-tier with no shared lib BY DECISION (a namespaced lib would be a third implementation — over-engineering); if MA ever grows a deployer that reads the same list, converge then. |

| 2026-08-03 | **NOT A CODE REVIEW — logged because the twin-port commit `fc7ccfaa` claims verification and the gate correctly demanded a witness.** Scope is ONE string: the `_SYSTEM_PROMPT` ratifier rubric in `src/mini_dudeai/cadence_fallback.py`, which is BYTE-LOCKED to MeshForge — this is the identical edit, not a re-derivation. Nothing else in the MA range was read adversarially; **treat this range as still unreviewed.** | **Controlled empirical A/B, run on the MF side** (the box that hosts the local brain; MA has no cadence_fallback test file and no ollama host): 2 cases x OLD/NEW prompt x 3 reps, sequential on an idle box, same corpus, only the prompt varying; conditions marker-verified per block, working tree restored via trap. | `fc7ccfaa` (MA) + `1334a84c` (MF) | **CAUSAL**: OLD 1/3 passing and needing all 3 attempts even then; NEW 3/3, mostly try 1/3. Precision held — the adversarial `triage-mixed-ratifiable-and-wrong` still passes try 1/3. **NOT a retrieval gap**: the corroborating `[Errno 24]` was verified inside the 700-char clamp in both root sets, so the model had the evidence and withheld it; the defect was prompt balance. **MA-SPECIFIC RESIDUAL**: MA has NO test file for `cadence_fallback.py` (MF has 40 tests), so this byte-locked module is covered here only by the regression guards and the parity check — an MA-side behavioural regression in it would not be caught locally. |

## Known NEVER-reviewed (as of 2026-07-06)

- The map HTTP surface (row 1) and the collector + fleet-observability +
  prometheus layer (row 2) have now had an adversarial pass. What remains
  NEVER-reviewed: the gateway / RNS-bridge / TUI / automation-engine /
  meshcore-connection paths — field soak + the test suite are their evidence
  base. Any commit outside the two ranges above is unreviewed.
- (RESOLVED 2026-07-06) The MeshForge maps-QA DoS/clamp findings
  (limit/window/open-redirect/path-traversal/weather-lock/dest-clamp) are now
  PORTED — see the 2026-07-06 row. What remains genuinely unreviewed is the rest
  of MA's map surface beyond the specific MF-ported findings (no independent
  adversarial pass over MA's collectors / federation / prometheus equivalents).

## Conventions for future passes

1. When a review pass closes, add its row HERE in the same commit as the fixes.
2. Hand every new pass the residual/refuted pointers of overlapping prior rows.
3. A fix commit is unreviewed code: re-review it before push, and record the
   re-review in the row.
4. A PORT from MeshForge covers only the ported findings — say so, so the rest
   of the MA file stays legibly "never-reviewed."

## Frontier worklist — queued upshift ranges (added 2026-07-16 with the upshift-witness gate port)

> The model advisor's upshift path lands HERE: when a session judges work
> review-shaped but isn't running on a frontier-class model, it appends a row
> instead of faking the pass (enforced by `scripts/review_provenance_check.py`
> in the pre-push hook — twin of MeshForge's, ported 2026-07-16 after its
> first live firing there). Next frontier session: pick from the top. Remove
> a row only by running the pass (its results become a row in the table above).
>
> Note: the 2026-07-13→16 Opus-interregnum range of THIS repo
> (`417d57ad..a1f32f93`) was already reviewed by the MeshForge-side pass
> (MF ledger row 2026-07-16) — MA scope was in that row, twin-first.

| Pri | Scope | Why it's frontier-shaped |
|-----|-------|--------------------------|
| Pri-2 | **The deployment profile is OVERLOADED — one flag set drives BOTH the TUI menu and daemon feature enablement.** `src/utils/deployment_profiles.py` feature_flags, `src/daemon_config.py` `_apply_profile` + `_load_yaml`, and every live reader of the saved profile (`src/daemon.py`, `src/utils/health_api.py`, `src/utils/map_data_service.py`, `src/utils/prometheus_exporter.py`). Surfaced 2026-09-16 while porting MeshForge's menu gating (`183c2b97`): `_apply_profile` maps gateway/meshtastic/rns onto `gateway_enabled`, mqtt onto `mqtt_enabled` and maps onto `map_server_enabled`, so choosing a narrower profile to get an honest MENU also switches daemon features off. Worked around on meshanchor-server with a system daemon config pinning the three values (YAML outranks profile defaults); the workaround is NOT the cure and is the only thing standing between that box and a silently degraded gateway. | A DESIGN question, not a bug with an obvious fix — which is exactly the shape a smaller model should not decide alone. Two readings are defensible and they disagree: either a profile is a DECLARATION OF PURPOSE (and switching daemon features off is correct, the menu merely reflecting it), or menu visibility and runtime enablement are separate concerns that happen to share a vocabulary. The evidence for the second: the flags are consumed with different MEANINGS — `mark_label` asks 'should this row say [off]' while `_apply_profile` asks 'should this subsystem run', and a newcomer changing a profile in the TUI to see more menu entries would silently re-enable daemon subsystems. ⚠️ The failure is this project's own signature class: a degraded state wearing a valid-looking value, deferred to the next restart so nobody connects cause to effect. Worth attacking: (a) whether any in-app path lets a user change the profile without being told what it does to the daemon; (b) whether the YAML-outranks-profile precedence is load-bearing anywhere else or just this rescue; (c) whether `load_or_detect_profile` in the daemon/health/exporter readers can hand a DETECTED profile to `_apply_profile` and disable a subsystem from a guess rather than a declaration. |
| — | _(add new upshift rows here as sessions surface review-shaped work)_ | |
