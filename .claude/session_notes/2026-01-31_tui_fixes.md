# Session Notes: 2026-01-31 TUI & NomadNet Fixes

## Branch: `claude/fix-fresh-install-issues-8HZ8m`

## Session 2 Commits (all pushed)

| Commit | Description |
|--------|-------------|
| `64aaa74` | Launch NomadNet as real user when running via sudo |
| `b4bf37c` | Add missing [textui] section to NomadNet config |
| `4bf9575` | Update session notes |
| `e3d6a0e` | Use plain text status indicators in whiptail menu |

## Issues Fixed This Session

### 1. ANSI Color Codes in Menu Header
- **Problem**: Whiptail showed raw escape codes `^[[32m●^[[0m`
- **Fix**: Added `plain=True` param to `get_status_line()`
- **Files**: `startup_checks.py`, `main.py`

### 2. NomadNet Missing [textui] Section
- **Problem**: Config had `user_interface = text` but no `[textui]` section
- **Fix**: Added section to config template, added `_validate_nomadnet_config()` auto-repair
- **File**: `nomadnet_client_mixin.py`

### 3. NomadNet Running as Root
- **Problem**: When MeshForge runs via sudo, NomadNet used `/root/.nomadnetwork/config`
- **Fix**: Launch with `sudo -u $SUDO_USER -i` to run as real user
- **File**: `nomadnet_client_mixin.py`

## Outstanding Issue (Not MeshForge)

### NomadNet urwid COLORMODE_16 Error
```
module 'nomadnet.ui' has no attribute 'COLORMODE_16'
```
- **Cause**: urwid library version incompatibility in NomadNet 0.9.8
- **Fix**: Reinstall NomadNet: `pipx uninstall nomadnet && pipx install nomadnet`
- **Status**: Upstream NomadNet issue, not MeshForge

---

## Session 1 Summary (Previous - Merged)

Branch: `claude/fix-meshtastic-interface-zbX5V` (merged)

1. rnsd meshtastic module - auto-install when detected
2. rnsd systemd detection - fixed enabled state check
3. HAT detection in wizard - check existing config.d
4. SWPC 404 fix - use text endpoint
5. TUI separator fix - removed Unicode that broke whiptail

---

## Next Session Pickup

1. Test NomadNet after user reinstalls via pipx
2. Consider adding urwid version check to MeshForge's NomadNet install
3. Any remaining fresh install issues
